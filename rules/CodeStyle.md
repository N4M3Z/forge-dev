Self-documenting code is non-negotiable. Names (functions, methods, classes, files) ARE the documentation. If code needs a comment to explain what it does, the name is wrong.

Don't add comments, docstrings, or type annotations to explain obvious intent.
