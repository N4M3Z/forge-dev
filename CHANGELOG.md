# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/).

## [Unreleased]

### Added

- `DomainHunt` skill with companions (`TldHacks`, `BrokerDetection`, `RegistrarGuide`, `BrandFamilyPairing`) — RDAP-based domain availability workflow, broker-listing detection, brand-family pairing strategy, registrar selection guidance
- `bin/check-domain` — bundled RDAP-based availability checker (bash + curl + python3), source: <https://github.com/N4M3Z/check-domain>
- `DEV-0005 Forge Ecosystem Domain Identity` ADR capturing the decision to anchor on `forgeworld.ai` with `modforge.*` and `handforged.*` sub-brands
- LICENSE (EUPL-1.2), CONTRIBUTING.md, CODEOWNERS, INSTALL.md
- `.gitattributes`, `.gitleaks.toml`, `.gitleaksignore`
- `.pre-commit-config.yaml` + `.githooks/pre-commit` with forge-cli validate.sh fallback
- `.mdschema` at repo root, `rules/.mdschema`, `skills/.mdschema`
- `.github/workflows/quality.yaml` and `release.yaml`
- `.gitlab-ci.yml`
- Adopted companion material from [davila7/claude-code-templates](https://github.com/davila7/claude-code-templates) at commit `d8e7e60f` under MIT → EUPL-1.2. Each adopted file ships with a SLSA provenance sidecar.
  - **Skill companions** (`@` included in existing SKILL.md): `CodeCleanup/CleanCode.md`, `DefensiveProgramming/CodeReviewChecklist.md`, `FixIssue/ErrorResolver.md`, `GitHub/GitHubWorkflowAutomation.md`, `ReceiveReview/ReceivingCodeReview.md`, `TestDrivenDevelopment/TestingAntiPatterns.md`, `SystematicDebug/{ConditionBasedWaiting,RootCauseTracing,DefenseInDepth,TestAcademic,TestPressure1,TestPressure2,TestPressure3}.md` + `find-polluter.sh`
  - **New skills**: `RequestingCodeReview`, `SubagentDrivenDevelopment`, `FixTests` (from upstream `test-fixing`), `FinishBranch` (from `finishing-a-development-branch`), `FixCI` (from `gh-fix-ci`). Upstream `writing-plans` and `executing-plans` rejected — duplicate forge-core's `WritePlan` and `ExecutePlan`. Upstream `using-git-worktrees` relocated to forge-core's `VersionControl/GitWorktrees.md` companion.
  - **New agents**: `CodeReviewer`, `RefactoringSpecialist`, `ErrorDetective`, `ArchitectReviewer`

### Changed

- forge-core `AdoptArtifact/SKILL.md` — removed the 3–5 initial cap and the skills-only restriction; agents are now eligible for adoption
- Provenance sidecars updated to reflect forge-core skill rename: `ForgeAdopt` → `AdoptArtifact` across agents and skill companions

### Removed

- `BashConventions` skill (with adopted `BashPatterns.md` companion) relocated to forge-core alongside `MarkdownConventions` — general shell conventions apply to every module, not just dev discipline
- `StatusLine` skill relocated to forge-util — managing the Claude Code statusline is tool-utility, not dev discipline
- Legacy `Git` skill retired; commit/staging/governance content absorbed into forge-core's `VersionControl` skill (with `GitHub.md`, `GitLab.md`, `GitWorktrees.md` companions covering platform-specific repo governance)
- `lib/` forge-lib submodule and `.gitmodules` — forge-cli's `forge` binary now handles install/assemble/validate directly, superseding the shared Makefile fragments that the submodule provided (see DEV-0004)
- `Makefile` forge-lib include chain — replaced with a lean 4-target wrapper (`install`, `validate`, `clean`, `help`) matching forge-cli's init template

### Kept with clarified scope

- `GitHub` skill remains in forge-dev for CI and sophisticated operational workflows — Actions/CI (`GitHubActions.md` companion), releases, PR automation, branch/label management, and raw API patterns. It is distinct from forge-core's `VersionControl/GitHub.md` companion, which covers only branch-protection rulesets and CODEOWNERS

## [0.1.0]

### Added

- Initial release with five skills: CodeCleanup, DefensiveProgramming, RustDevelopment, TestDrivenDevelopment, Git
- `.claude-plugin/plugin.json` for Claude Code plugin discovery
- Provider directories (`.claude/`, `.gemini/`, `.codex/`, `.opencode/`) generated by `make install`
